# Исправление переноса вариантов — 13 сентября 2026

После обратной связи восстановлены исходные значения из commit 71901239a970daf8d190d1df0ed0989d47c5ee8c:

- Пороги и все поля матрицы 8 семестра, без нормализации чисел.
- Интервалы журналов и сопутствующие поля 30 вариантов.
- 30 исходных профилей предприятий в выборе варианта. Эти профили относятся к подготовке к демоэкзамену; остальные работы сохраняют общую систему service_ops и собственные параметры.
- В разделе условий и CSV показаны поля конкретной работы вместо единственного общего имени базы.

Матрицы 6 и 7 семестров не изменялись. Расширения стендов и дополнительные наборы сохранены отдельно от исходных условий.

Проверка npm run check теперь сравнивает все исходные CSV с независимым снимком первоначального коммита (с нормализацией перевода строк) и каждую из 660 комбинаций с исходной строкой матрицы. Изменение условий блокирует проверку.

Браузер: переключение V01/V02/V30 в работах 6/7/8 семестров и подготовке к демоэкзамену изменяет параметры; главная проверена при 1366 и 390 пикселях без горизонтальной прокрутки и ошибок JavaScript.

Исходные несогласованности не исправлялись заменой условий: в журнале отсутствуют записи для части исходных окон, а некоторые пороги требуют уточнения единиц или направления. Это явно отмечено в материалах. Прежний отчёт о нормализации порогов и окон описывает отменённую версию 801d785; актуальная проверка архивов находится в archive-check.json.
